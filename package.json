{
  "name": "ci-cd-monorepo",
  "private": true,
  "scripts": {
    "ci:build": "lerna run build",
    "ci:test": "lerna run ci:test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
